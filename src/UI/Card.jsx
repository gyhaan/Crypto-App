function Card({ number }) {
  return (
    <div className="w-full h-[200px] mx-5 flex justify-center items-center bg-custom-blue-violet text-[#fff]">
      {number}
    </div>
  );
}

export default Card;
